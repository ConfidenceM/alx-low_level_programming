#include <stdio>

/*
 * main assign a random nummber to variable
 * and print last of number stored
 * return : always success
 */
int main(void)
{ 
	int ch;
	
for (ch = 48; ch <= 57; ch++)
	{
putchar(ch);
	}
	putchar(10);
	return (0);
}
